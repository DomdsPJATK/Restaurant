import React from 'react';
import { LabelStyled } from './styles/Label';

const Label = (props) => {
    return (
        <LabelStyled {...props}></LabelStyled>
    );
}

export default Label;